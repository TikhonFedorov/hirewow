"use strict";
'use client';
Object.defineProperty(exports, "__esModule", { value: true });
exports.RadioButton = void 0;
const tslib_1 = require("tslib");
const react_1 = require("react");
const jsx_runtime_1 = require("react/jsx-runtime");
const React = tslib_1.__importStar(require("react"));
const private_1 = require("../../hooks/private");
const cn_1 = require("../utils/cn");
const RadioButtonOption_1 = require("./RadioButtonOption");
require("./RadioButton.css");
const b = (0, cn_1.block)('radio-button');
exports.RadioButton = React.forwardRef(function RadioButton(props, ref) {
    const { size = 'm', width, style, className, qa, children } = props;
    let options = props.options;
    if (!options) {
        options = React.Children.toArray(children).map(({ props }) => ({
            value: props.value,
            content: props.content || props.children,
            disabled: props.disabled,
            title: props.title,
        }));
    }
    const plateRef = React.useRef(null);
    const optionRef = React.useRef();
    const handleCheckedOptionMount = React.useCallback((checkedOptionNode) => {
        if (!checkedOptionNode) {
            return;
        }
        const plateNode = plateRef.current;
        if (!plateNode) {
            return;
        }
        const uncheckedOptionNode = optionRef.current;
        if (uncheckedOptionNode && uncheckedOptionNode !== checkedOptionNode) {
            const setPlateStyle = (node) => {
                plateNode.style.left = `${node.offsetLeft}px`;
                plateNode.style.width = `${node.offsetWidth}px`;
            };
            setPlateStyle(uncheckedOptionNode);
            plateNode.hidden = false;
            setPlateStyle(checkedOptionNode);
        }
        optionRef.current = checkedOptionNode;
    }, []);
    const handlePlateTransitionEnd = (event) => {
        event.currentTarget.hidden = true;
    };
    const { containerProps, optionsProps } = (0, private_1.useRadioGroup)(Object.assign(Object.assign({}, props), { options }));
    return ((0, jsx_runtime_1.jsxs)("div", Object.assign({}, containerProps, { ref: ref, style: style, className: b({ size, width }, className), "data-qa": qa, children: [(0, jsx_runtime_1.jsx)("div", { ref: plateRef, className: b('plate'), onTransitionEnd: handlePlateTransitionEnd, hidden: true }), optionsProps.map((optionProps) => ((0, react_1.createElement)(RadioButtonOption_1.RadioButtonOption, Object.assign({}, optionProps, { key: optionProps.value, ref: optionProps.checked ? handleCheckedOptionMount : undefined }))))] })));
});
exports.RadioButton.Option = RadioButtonOption_1.RadioButtonOption;
