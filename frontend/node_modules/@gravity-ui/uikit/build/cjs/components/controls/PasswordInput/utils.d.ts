import type { ButtonSize } from '../../Button';
import type { InputControlSize } from '../types';
export declare const getActionButtonSizeAndIconSize: (textInputSize: InputControlSize) => {
    actionButtonSize: ButtonSize;
    iconSize: number;
};
