'use client';
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { useRadio } from '../../hooks/private';
import { block } from '../utils/cn';
import { isIcon, isSvg } from '../utils/common';
const b = block('radio-button');
export const RadioButtonOption = React.forwardRef(function RadioButtonOption(props, ref) {
    const { disabled = false, content, children, title } = props;
    const { checked, inputProps } = useRadio(props);
    const inner = content || children;
    const icon = isIcon(inner) || isSvg(inner);
    return (_jsxs("label", { className: b('option', {
            disabled,
            checked,
        }), ref: ref, title: title, children: [_jsx("input", Object.assign({}, inputProps, { className: b('option-control') })), _jsx("span", { className: b('option-outline') }), inner && _jsx("span", { className: b('option-text', { icon }), children: inner })] }));
});
