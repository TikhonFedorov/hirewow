import { __rest } from "tslib";
import { jsx as _jsx } from "react/jsx-runtime";
import { ListContainerView } from '../ListContainerView';
import { ListItemRecursiveRenderer } from '../ListRecursiveRenderer/ListRecursiveRenderer';
export function ListContainer(_a) {
    var { containerRef, renderItem, list } = _a, props = __rest(_a, ["containerRef", "renderItem", "list"]);
    return (_jsx(ListContainerView, Object.assign({ ref: containerRef }, props, { children: list.structure.items.map((item, index) => (_jsx(ListItemRecursiveRenderer, { itemSchema: item, id: list.structure.rootIds[index], list: list, children: renderItem }, index))) })));
}
