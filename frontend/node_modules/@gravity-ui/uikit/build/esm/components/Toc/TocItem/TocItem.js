'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { useActionHandlers } from '../../../hooks';
import { block } from '../../utils/cn';
import './TocItem.css';
const b = block('toc-item');
export const TocItem = (props) => {
    const { active = false, childItem = false, content, href, value, onClick, onItemClick } = props;
    const handleClick = React.useCallback((event) => {
        onItemClick === null || onItemClick === void 0 ? void 0 : onItemClick(event);
        if (value === undefined || !onClick) {
            return;
        }
        onClick(value);
    }, [onClick, onItemClick, value]);
    const { onKeyDown } = useActionHandlers(handleClick);
    const item = href === undefined ? (_jsx("div", { role: "button", tabIndex: 0, className: b('section-link'), onClick: handleClick, onKeyDown: onKeyDown, children: content })) : (_jsx("a", { href: href, onClick: handleClick, className: b('section-link'), children: content }));
    return _jsx("div", { className: b('section', { child: childItem, active }), children: item });
};
