import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { block } from '../utils/cn';
import './Divider.css';
const b = block('divider');
export const Divider = React.forwardRef(function Divider(props, ref) {
    const { orientation = 'horizontal', className, style, qa, children, align = 'start' } = props;
    return (_jsx("div", { className: b({ orientation, align }, className), ref: ref, style: style, "data-qa": qa, role: "separator", "aria-orientation": orientation === 'vertical' ? 'vertical' : undefined, children: children }));
});
