import { __rest } from "tslib";
import { jsx as _jsx } from "react/jsx-runtime";
import { Copy, CopyCheck, CopyXmark } from '@gravity-ui/icons';
import { Icon } from '../Icon';
export function ClipboardIcon(_a) {
    var { status } = _a, rest = __rest(_a, ["status"]);
    if (status === 'error') {
        return _jsx(Icon, Object.assign({ data: CopyXmark }, rest));
    }
    if (status === 'success') {
        return _jsx(Icon, Object.assign({ data: CopyCheck }, rest));
    }
    return _jsx(Icon, Object.assign({ data: Copy }, rest));
}
