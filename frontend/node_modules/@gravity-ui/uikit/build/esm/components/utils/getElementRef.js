// Access the element's ref using a method that doesn't produce a warning.
export function getElementRef(element) {
    var _a, _b;
    if (process.env.NODE_ENV !== 'production') {
        // Before React 19, there is an access check for ReactElement's props.ref in dev builds.
        const getter = (_a = Object.getOwnPropertyDescriptor(element.props, 'ref')) === null || _a === void 0 ? void 0 : _a.get;
        if (getter && 'isReactWarning' in getter && getter.isReactWarning) {
            return element.ref;
        }
    }
    return (_b = element.props.ref) !== null && _b !== void 0 ? _b : element.ref;
}
