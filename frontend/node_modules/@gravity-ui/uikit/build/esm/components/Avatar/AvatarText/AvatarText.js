import { jsx as _jsx } from "react/jsx-runtime";
import { getAvatarDisplayText } from './utils';
export const AvatarText = ({ text, color, className }) => {
    const style = { color };
    const displayText = getAvatarDisplayText(text);
    return (_jsx("div", { style: style, className: className, children: displayText }));
};
